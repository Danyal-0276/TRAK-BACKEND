# Core framework
Django>=5.2,<5.3
python-dotenv>=1.0,<2

# MongoDB as Django's primary DB (auth, sessions, contenttypes).
# django-mongodb-backend 5.2.x line tracks Django 5.2 LTS.
django-mongodb-backend==5.2.3
pymongo>=4.10,<5.0
dnspython>=2.6

# REST + JWT (stateless, no blacklist app)
djangorestframework>=3.16,<3.17
djangorestframework-simplejwt==5.5.1
django-cors-headers>=4.6,<5

# ASGI + static + websockets
whitenoise>=6.8,<7
daphne>=4.2,<5
channels>=4.3,<5
channels-redis>=4.2,<5

pytz>=2024.1

# Raw news ingestion (scrapers). curl_cffi mimics browser TLS (many CDNs block plain Python TLS).
curl_cffi==0.14.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11
