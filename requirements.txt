# TRAK Backend — pin versions in production as needed
Django>=3.2,<5
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3,<6
django-cors-headers
djongo>=1.3.6
pytz>=2023.3
sqlparse
gunicorn
dnspython
pymongo

# Raw news ingestion (scrapers). curl_cffi mimics browser TLS (many CDNs block plain Python TLS).
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11
channels>=4.1,<5
channels-redis>=4.2,<5
